#include <bits/stdc++.h>
using namespace std;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);

	// Read whatever the problem states, e.g.:
	int w;
	cin >> w;
	if (w > 2 && w % 2 == 0) {
		cout << "YES";
	} else{
		cout << "NO";
	}
		return 0;
}
