#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t; 
    if (!(cin >> t)) return 0;
    while (t--) {
        // Read this test's input, e.g.:
        // int n; cin >> n;
        // vector<long long> a(n); for (auto &x : a) cin >> x;
        // cout << /* answer */ << '\n';
    }
    return 0;
}
